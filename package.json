{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "description": "Portfolio monorepo with multiple project showcases",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:hotel": "cd apps/hotel-dashboard && npm run dev",
    "build:hotel": "cd apps/hotel-dashboard && npm run build",
    "install:all": "npm install && cd apps/hotel-dashboard && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "firebase": "^12.7.0"
  }
}
